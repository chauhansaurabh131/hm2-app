import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const DemoCode = () => {
  return (
    <SafeAreaView>
      <Text>cnkl</Text>
    </SafeAreaView>
  );
};

export default DemoCode;
